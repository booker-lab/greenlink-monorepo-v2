# 🌿 그린링크 모노레포 v2

**하이퍼로컬 화훼/농수산 직거래 플랫폼**

> ⚠️ 이 저장소는 `greenlink v1`의 후속 버전입니다. v1과 별개의 멀티레포 구조입니다.

---

## 📁 프로젝트 구조

```
greenlink-monorepo/
├── apps/
│   ├── web/           # 소비자용 PWA (포트 3000)
│   │   └── src/app/   # 홈, 카테고리, 장바구니, 마이페이지
│   └── admin/         # 농가/판매자 대시보드 (포트 3001)
│       └── src/app/   # 비즈프로필, 상품등록, 소식관리
├── packages/
│   ├── ui/            # 공용 UI 컴포넌트 (Button, Card, Input)
│   └── lib/           # API 클라이언트, 결제 모듈(Toss)
├── turbo.json         # Turborepo 빌드 파이프라인
├── pnpm-workspace.yaml
└── package.json
```

---

## 🚀 시작하기

### 설치
```bash
pnpm install
```

### 개발 서버 실행
```bash
# 전체 앱 실행
pnpm dev

# 소비자 앱만 (포트 3000)
pnpm --filter @greenlink/web dev

# 농가 대시보드만 (포트 3001)
pnpm --filter @greenlink/admin dev
```

### 빌드
```bash
pnpm build
```

---

## 📱 앱 설명

### `/apps/web` - 소비자용 PWA
일반 사용자가 농산물을 찾아보고 구매하는 앱
- 홈 화면 (배너, 추천 상품)
- 카테고리 탐색
- 장바구니 / 결제
- 마이페이지

### `/apps/admin` - 농가 대시보드
농가/판매자가 상품을 등록하고 관리하는 앱
- 비즈프로필 홈 (그린 온도, 단골 수)
- 간편 상품 등록 (사진 + 가격 + 수량)
- 소식 쓰기 / 리뷰 관리

---

## 📦 공용 패키지

### `@greenlink/ui`
- `Button` - primary/secondary/outline 변형
- `Card` - 카드 레이아웃
- `Input` - 라벨, 에러, suffix 지원

### `@greenlink/lib`
- `api` - Axios 기반 API 클라이언트
- `payment` - Toss Payments 연동 모듈

---

## 🛠 기술 스택

| 구분 | 기술 |
|------|------|
| Framework | Next.js 15 (App Router) |
| Monorepo | Turborepo + pnpm |
| Styling | Tailwind CSS |
| State | Zustand |
| Payment | Toss Payments |

---

## 📋 v1 → v2 마이그레이션

v1에서 사용하던 코드를 v2로 옮기려면:

1. `greenlink v1/src/components/` → `apps/web/src/components/`
2. `greenlink v1/src/app/` → `apps/web/src/app/`
3. 공통 컴포넌트는 `packages/ui/`로 분리

---

## 📄 라이선스

MIT License
